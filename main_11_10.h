/* Jalen Jarrett
COP 3223C
Nov 9, 2025
Calling each function from main.c and declaring all variables
*/

#define MAXSTRINGS 100
#define MAXLENGTH 51
void selection_sort(char str[][MAXLENGTH], int numOfStrings);
void lower_str_converter(char *str);
void swap(char *str1, char *str2);